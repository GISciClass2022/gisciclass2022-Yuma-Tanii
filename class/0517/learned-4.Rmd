---
title: "Chap6"
author: "Yuma Tanii"
date: "2022/5/17"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown
```{r ready}
library(sf)
library(raster)
library(dplyr)
library(spData)
library(spDataLarge)
```
]
```{r 08-mapping-2, message=FALSE}
library(tmap)    # for static and interactive maps
library(leaflet) # for interactive maps
library(ggplot2) # tidyverse data visualization package
```

```{r 7}
adm = st_read("JPN_adm/JPN_adm1.shp")
rds = st_read("JPN_rds/JPN_roads.shp")
pop = st_read("JPN_msk_pop/jpn_msk_pop.vrt")

map_jp = tm_shape(adm) + tm_polygons()
map_jp2 = tm_shape(rds) + tm_lines(lwd=0.1)
map_jp3 = tm_shape(adm) + tm_polygons() + tm_shape(rds) + tm_lines(lwd=0.1)
map_jp3
```

















